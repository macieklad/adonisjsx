{
  "name": "macieklad/adonisjsx",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter adonisjsx build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm --filter adonisjsx typecheck",
    "test": "pnpm --filter adonisjsx-e2e test",
    "changeset": "changeset",
    "release": "changeset version && pnpm build && cp README.md ./packages/adonisjsx/ && changeset publish"
  },
  "devDependencies": {
    "syncpack": "^13.0.3"
  },
  "dependencies": {
    "@changesets/cli": "^2.28.1"
  }
}
